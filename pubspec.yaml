name: gstock
description: "A new Flutter project."
publish_to: 'none'  # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  firebase_core: ^2.12.0
  qr_flutter: ^4.0.0
  firebase_auth: ^4.4.1
  cloud_firestore: ^4.11.0
  flutter_screenutil: ^5.0.0
  google_nav_bar: ^5.0.0
  get: ^4.6.5
  qr_code_scanner: ^0.7.0  # QR code scanner dependency

  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
